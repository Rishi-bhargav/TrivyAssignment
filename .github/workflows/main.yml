name: Docker Build and Push
on:
  workflow_dispatch:
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:      
      - name: Build Docker image
        run: docker build -t quotegenerator:v1 .
